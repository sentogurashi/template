// TODO: 仮
/*
import $ from 'jquery';

const fixNavigationHeight = (e) => {
  $('.Navigation').height($(window).height());
};
$(window).on('resize', fixNavigationHeight);
$(() => {
  fixNavigationHeight();
});
*/
